<resources>
    <!-- ... your existing styles ... -->

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:background">@color/dark_gray</item> <!-- Dark Gray background -->
        <item name="android:textColor">@color/white</item> <!-- White text -->
        <item name="android:textColorHint">@color/gray</item> <!-- Gray hint text -->
        <item name="android:editTextColor">@color/white</item>
    </style>

    <style name="RatingBar"></style>

</resources>